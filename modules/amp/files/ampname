# Put the name of this amp monitor in this file.  You may also use the
# special names USE HOSTNAME or USE DNS NAME in which case the amp
# name will be constructed from the hosts hostname or DNS name with the
# AMP_NAME_PREFIX (amp- by default) prepended.
# You can also add NO PREFIX if you do not want the AMP_NAME_PREFIX added

#Note: If you change this value, amp must be restarted.


USE HOSTNAME
