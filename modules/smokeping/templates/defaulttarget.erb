<%
    # Downcase all information
    dom = domain.downcase
    host = hostname.downcase
    fhost = name.downcase
%>
+ <%= host %>
    menu = <%= fhost %>
    title = <%= fhost %>
    host = <%= fhost %>

